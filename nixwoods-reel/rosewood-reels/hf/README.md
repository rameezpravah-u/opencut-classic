# Product photography

Live PDP images pulled from the Shopify CDN on 6 Sep 2026 with `graphql_query` on the product handle. **Real photography, not generated** — which is why every one is marked approved in `system/presets.json`; the fidelity gate exists for generated frames, and these came from the store.

Re-pull them any time with the handle in `system/presets.json`; the CDN URLs are public and stable. Anything the store later replaces on the PDP should be re-pulled here so the reels and the product page never disagree.
